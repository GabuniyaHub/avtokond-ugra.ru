export interface Buses {
    id: number;
    model: string;
    category: string;
    datetime: string;
    owner: string;
    regNumber: string;
    createdAt: string;
    updateUp: string;
}

export interface serveStatic {
    
}